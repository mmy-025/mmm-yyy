# Pyodide代码编辑器改造任务清单

## 任务列表

### [x] Task 1: 完善pyodide.ts工具函数
**优先级**: P0  
**依赖**: 无

**任务描述**:
- 确保initPyodide()正确初始化Pyodide
- 实现runPythonCode()执行代码并捕获输出
- 实现generateChart()生成matplotlib图表
- 配置matplotlib使用AGG后端支持前端渲染
- 预装所有数据分析库

**验收标准**:
- [ ] Pyodide能正确加载
- [ ] 代码执行返回结果或错误
- [ ] 图表能正确生成

**关键约束**:
- ❌ 不修改任何UI布局和样式
- ✅ 只修改功能逻辑代码

---

### [x] Task 2: 改造KnowledgeDetail.tsx代码编辑器部分
**优先级**: P0  
**依赖**: Task 1

**任务描述**:
- 保留所有现有的div结构和样式类名
- 保留Monaco Editor组件和配置
- 保留运行按钮和重置按钮的样式和位置
- 集成新的Pyodide运行函数
- 改进输出展示区域，支持：
  - 文本输出
  - DataFrame表格展示
  - 图表显示
  - 错误信息展示
- 保持h-96高度的编辑器
- 保持flex布局和所有间距类

**验收标准**:
- [ ] 编辑器显示在原有位置
- [ ] 编辑器高度保持h-96
- [ ] 运行按钮样式和位置不变
- [ ] 输出区域显示正确
- [ ] 代码能正确执行并显示结果

**关键约束**:
- ❌ 禁止修改任何布局类（flex、w-*、h-*）
- ❌ 禁止修改任何样式类（bg-*、text-*、rounded-*）
- ❌ 禁止修改任何间距类（p-*、m-*）
- ❌ 禁止修改响应式类（md:*、lg:*）
- ✅ 只允许修改功能逻辑和事件处理函数

---

### [x] Task 3: 同步改造Learn.tsx代码编辑器部分
**优先级**: P0  
**依赖**: Task 1

**任务描述**:
- 与KnowledgeDetail.tsx保持一致的改造
- 保留所有现有的div结构和样式类名
- 保留Monaco Editor组件和配置
- 集成新的Pyodide运行函数
- 改进输出展示区域

**验收标准**:
- [ ] 编辑器显示在原有位置
- [ ] 运行按钮样式和位置不变
- [ ] 代码能正确执行并显示结果

---

### [x] Task 5: 构建和部署验证
**优先级**: P1  
**依赖**: Task 2, Task 3

**任务描述**:
- 测试KnowledgeDetail页面代码编辑器
- 测试Learn页面代码编辑器
- 测试代码执行功能
- 测试图表生成功能
- 测试错误处理功能
- 测试Pyodide加载状态显示

**验收标准**:
- [ ] 所有页面代码编辑器正常工作
- [ ] 代码执行无报错
- [ ] 图表能正确显示
- [ ] 布局和样式完全保持不变

---

### [ ] Task 5: 构建和部署验证
**优先级**: P1  
**依赖**: Task 4

**任务描述**:
- 运行npm run build构建项目
- 验证构建成功
- 验证dist目录生成
- 确认所有功能正常

**验收标准**:
- [ ] 构建成功无错误
- [ ] 功能完整无报错
- [ ] 可以正常部署上线

---

## 任务依赖关系

```
Task 1 (完善pyodide.ts)
    ↓
Task 2 (改造KnowledgeDetail) ─┐
    ↑                          │
Task 3 (改造Learn) ────────────┘
    ↓
Task 4 (测试验证)
    ↓
Task 5 (构建部署验证)
```

## 关键检查点

### 🚨 改造前必读

1. **KnowledgeDetail.tsx第246-339行**是代码编辑器部分，改造时只能修改这部分
2. **Learn.tsx类似区域**也需要同步改造
3. **pyodide.ts**需要完善，提供完整的运行功能

### ✅ 改造完成后自检

- [ ] 编辑器是否在页面原有位置？
- [ ] 编辑器高度是否仍然是h-96？
- [ ] 运行按钮样式是否完全一致？
- [ ] 布局结构是否完全未改变？
- [ ] 所有flex布局类是否保持不变？
- [ ] 所有间距类是否保持不变？
- [ ] 所有响应式类是否保持不变？

### 🧪 功能测试清单

- [ ] Pyodide环境加载成功
- [ ] 导入pandas成功
- [ ] 导入numpy成功
- [ ] 导入matplotlib成功
- [ ] 创建DataFrame成功
- [ ] DataFrame打印显示正确
- [ ] matplotlib图表生成正确
- [ ] 错误信息正确显示
- [ ] 代码运行响应时间<5秒
